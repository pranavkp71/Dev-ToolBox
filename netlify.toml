[build]
  command = "CI= npm run build"
  publish = "dist"
  environment = { NODE_VERSION = "18" }
  command = "npm run build"
  publish = "dist"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200